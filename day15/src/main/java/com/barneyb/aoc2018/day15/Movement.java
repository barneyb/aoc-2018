package com.barneyb.aoc2018.day15;

class Movement {

    void doRound(Map m) {
        for (Unit u : m.units()) {
            // if not alive, do nothing
            if (! u.alive()) continue;
            // if adjacent to enemy, do nothing
            // find all enemies
            // find all positions adjacent to an enemy
            // ignore the ones that contain a unit
            // if no positions remain, do nothing
            // calculate path length for each position
            // find positions with shortest path
            // find first position in reading order
            // find all paths to that position
            // find shortests paths
            // take firs
        }
    }

}
